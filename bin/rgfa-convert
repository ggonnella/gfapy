#!/usr/bin/env ruby
require "rgfa"

if ARGV.size != 1
  STDERR.puts "Convert a GFA file to the other specification version"
  STDERR.puts
  STDERR.puts "Usage: #$0 <gfafile>"
  exit 1
end

gfa = RGFA.from_file(ARGV[0])
if gfa.version == :gfa1
  puts gfa.to_gfa2_s
else
  puts gfa.to_gfa1_s
end
